const initialState = {
  uuid: 0,
  name: '',
  imageURL: '',
}

const photo = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default photo;
